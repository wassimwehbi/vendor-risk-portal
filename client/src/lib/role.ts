// Deprecated. Identity and role now come from the authenticated session
// (see AuthContext.tsx / the server's /api/auth endpoints). This file is kept
// intentionally empty to avoid reintroducing client-side, self-asserted roles.
export {};
