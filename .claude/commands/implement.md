# Implement the following plan
Follow the `Instructions` to implement the `Plan` then `Report` the completed work.

## Instructions
- Read the plan, think hard about the plan and implement the plan.
- Follow existing patterns and conventions in the codebase. Respect TypeScript strict mode and the shared `card` / `btn-*` / `input` / `label` utilities for any UI.
- IMPORTANT: This is a vendor-risk app. The final vendor-risk decision is always made by a human analyst, never automatically by the AI. Preserve that contract.
- IMPORTANT: After implementing the plan, run `npm run format:fix` (Biome write) from the repo/worktree root so the generated TypeScript under `server/src` and `client/src` passes `npm run check`.

## Plan
$ARGUMENTS

## Report
- Summarize the work you've just done in a concise bullet point list.
- Report the files and total lines changed with `git diff --stat`.
