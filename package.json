{
  "name": "vendor-risk-portal",
  "private": true,
  "license": "UNLICENSED",
  "version": "0.1.0",
  "description": "AI-Assisted Vendor Risk Questionnaire Analysis Tool (SIG -> ISO 27001 / ISO 27002 / GDPR)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install-all": "npm --prefix server install && npm --prefix client install",
    "install:ci": "npm ci && npm ci --prefix server && npm ci --prefix client",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "check": "biome ci .",
    "lint": "biome lint .",
    "format": "biome format .",
    "format:fix": "biome check --write .",
    "test": "npm --prefix server test && npm --prefix client test",
    "test:coverage": "npm --prefix server run test:coverage && npm --prefix client run test:coverage",
    "test:e2e": "playwright test",
    "test:ux": "playwright test --config=playwright.ux.config.ts",
    "test:ux:update": "UX_VISUAL=1 playwright test --config=playwright.ux.config.ts --update-snapshots",
    "prepare": "lefthook install || true"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@biomejs/biome": "^2.0.0",
    "@playwright/test": "^1.49.1",
    "concurrently": "^9.1.0",
    "lefthook": "^1.10.0"
  }
}
